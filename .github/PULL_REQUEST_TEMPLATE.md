**What this changes (user-visible effect)**

**Checklist**

- [ ] `dotnet test` passes locally
- [ ] `dotnet format --verify-no-changes` passes
- [ ] Golden snapshots updated deliberately (diff reviewed) if rendering changed
- [ ] CHANGELOG.md Unreleased section updated
- [ ] No new network calls, no telemetry, security posture unchanged
