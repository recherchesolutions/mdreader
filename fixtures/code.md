# Code blocks

Inline `code` and a fenced block with language info:

```csharp
public static int Add(int a, int b)
{
    // Adds two numbers.
    return a + b;
}
```

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"
```

A block with no language:

```
plain text block
with two lines
```

An indented code block:

    indented code
    second line
